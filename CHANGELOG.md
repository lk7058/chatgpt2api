# Changelog

本项目基于 [basketikun/chatgpt2api](https://github.com/basketikun/chatgpt2api) 二次开发。

## [1.0.0] - 2026-08-18

### 新增

- 版本显示与更新检测（仅管理员可见）：顶栏 GitHub 右侧显示当前版本，点击弹出版本信息（版本记录 / 更新说明 / 是否有新版）
- 纯邮箱账号体系：邮箱注册 / 登录 / 找回密码 / 用户中心绑定邮箱
- 签到与连续签到奖励（里程碑档位可配置：如满 3 天 +5、满 7 天 +20）
- 额度系统：注册赠送 / 签到赠送 / 管理员分配 / 充值卡兑换 / 额度流水
- 找回密码流程（绑定邮箱验证码重置，重置后自动注销全部会话）
- 登录防爆破（IP + 账号维度失败锁定）、验证码发送限流与冷却
- 服务端图片 URL 抓取 SSRF 防护（拒绝内网/保留地址）
- 配置接口密钥脱敏（SMTP / R2 / WebDAV 凭据不回传明文）
- 会话 30 天自动过期、密码最少 8 位
- 静态资源 gzip + 长缓存头、安全响应头（nosniff / DENY / no-referrer）
- 管理面板：用户管理 / 图片管理 / 备份 / 日志 / 第三方 API / Sub2API / CPA
- 定制版 README（安装教程 + 原作者声明）

### 调整

- Docker 启动命令改为 `uv run --no-sync`（避免启动时联网重新同步依赖）
- docker-compose 镜像指向本地构建的 `chatgpt2api:custom`，不再默认拉取上游镜像
- 会话 token 存储改为 sha256 哈希 + 内存缓存（降低每次请求 CPU/磁盘开销）
- 图片列表/缩略图改在线程池执行（避免阻塞事件循环）

### 修复

- 修复找回密码接口泄露邮箱注册状态（统一返回文案防枚举）
- 修复第三方 API 错误原样回显（脱敏处理）
- 修复登录/注册接口无速率限制的问题
- 修复静态资源缓存头与 gzip 缺失的问题
- 修复容器内日志时间为 UTC 的问题（Docker 默认时区改为 Asia/Shanghai，可用 TZ 环境变量覆盖）
