# Changelog

本项目基于 [basketikun/chatgpt2api](https://github.com/basketikun/chatgpt2api) 二次开发。

## [1.0.2] - 2026-08-21

### 新增

- **对外 API 模块**：用户中心新增「API」标签页（签到 / 额度 / 安全 / API / MCP 五个标签页结构）
  - 接口接入说明：OpenAI 兼容接口文档（模型列表 / 聊天补全 / Responses / 搜索 / 图片生成编辑 / PPT / PSD / 任务查询 / 文件下载），展示管理员配置的「可 API 调用的模型」
  - API Key 管理：支持生成多个 Key（均绑定当前用户）、单 Key 绑定单模型（下拉选择管理员开放模型）、停用/启用/删除、查看每次调用的时间 / IP / 接口 / 模型 / 状态 / 耗时
  - 调用日志全链路记录客户端 IP（含图片任务后台日志与续轮询日志）
  - 管理员端：对外 API 全局开关（关闭后 API Key 调用立即 403，站内前端不受影响）、单用户 API 开关 / 并发上限 / 每日调用次数上限（0=不限）、日志页新增 IP 列
  - API 可用模型由管理员在「设置 → 接口接入」用选择器配置（用户中心 Key 绑定下拉与接口文档同步展示），未配置时回退默认列表
  - 后台不再提供 Key 创建入口（移除「用户密钥」tab 与相关端点），API Key 仅由用户在用户中心自行创建
- 第三方 API 画图模型支持按分辨率档位（1k/2k/4k）配置倍率定价，任务扣费 / 预检 / 退还按档位计算
- 第三方 API 支持配置代理地址，请求（含获取模型/测试连接）走代理绕过上游 IP 封锁
- 用户中心新增「模型广场」：展示模型名称 / 类型（画图/对话）/ 来源（账号池/第三方）/ 计费倍率（画图按分辨率档位）
- 邮箱模板设置：注册 / 找回 / 绑定 / 测试四场景模板 CRUD + 变量渲染 + HTML 邮件
- 接口接入说明彻底删除会话 Key 展示（不再暴露登录会话 token）

### 修复

- 第三方 API 画图模型 2k/4k 宽高比档位仅 codex 可用的限制移除，改由管理员按模型配置
- 第三方 API 模型定价输入排版：画质 / 倍率分两行展示，避免输入框被相邻模型遮挡

### 其他

- 画布入口并入基础配置；接口文档模型区块标题改为「可API调用的模型」

## [1.0.1] - 2026-08-19

### 新增

- MCP 服务支持：Streamable HTTP 端点 `/mcp` + Key 鉴权（Bearer），工具 `generate_image` / `get_quota`；用户中心「MCP 接入」自助生成/复制 Key 与客户端配置，设置页「MCP 服务」全局开关 / 逐用户 Key 管理 / 调用日志

### 修复

- 用户中心各 Tab 页面尺寸不统一：容器宽度与首页统一，四个分区（签到 / 额度 / 安全 / MCP）内容区统一最小高度，切换不再跳动
- 登录/注册后默认打开首页

### 其他

- `/api/version` 增加管理员鉴权（版本信息仅管理员可见）
- 反馈邮箱：l@w.cx

## [1.0.0] - 2026-08-18

### 新增

- 版本显示与更新检测（仅管理员可见）：顶栏 GitHub 右侧显示当前版本，点击弹出版本信息（版本记录 / 更新说明 / 是否有新版）
- 纯邮箱账号体系：邮箱注册 / 登录 / 找回密码 / 用户中心绑定邮箱
- 签到与连续签到奖励（里程碑档位可配置：如满 3 天 +5、满 7 天 +20）
- 额度系统：注册赠送 / 签到赠送 / 管理员分配 / 充值卡兑换 / 额度流水
- 找回密码流程（绑定邮箱验证码重置，重置后自动注销全部会话）
- 登录防爆破（IP + 账号维度失败锁定）、验证码发送限流与冷却
- 服务端图片 URL 抓取 SSRF 防护（拒绝内网/保留地址）
- 配置接口密钥脱敏（SMTP / R2 / WebDAV 凭据不回传明文）
- 会话 30 天自动过期、密码最少 8 位
- 静态资源 gzip + 长缓存头、安全响应头（nosniff / DENY / no-referrer）
- 管理面板：用户管理 / 图片管理 / 备份 / 日志 / 第三方 API / Sub2API / CPA
- 定制版 README（安装教程 + 原作者声明）

### 调整

- Docker 启动命令改为 `uv run --no-sync`（避免启动时联网重新同步依赖）
- docker-compose 镜像指向本地构建的 `chatgpt2api:custom`，不再默认拉取上游镜像
- 会话 token 存储改为 sha256 哈希 + 内存缓存（降低每次请求 CPU/磁盘开销）
- 图片列表/缩略图改在线程池执行（避免阻塞事件循环）

### 修复

- 修复找回密码接口泄露邮箱注册状态（统一返回文案防枚举）
- 修复第三方 API 错误原样回显（脱敏处理）
- 修复登录/注册接口无速率限制的问题
- 修复静态资源缓存头与 gzip 缺失的问题
- 修复容器内日志时间为 UTC 的问题（Docker 默认时区改为 Asia/Shanghai，可用 TZ 环境变量覆盖）
